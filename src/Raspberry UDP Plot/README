
Shows a plot of numeric lines sent to UDP port. 
(set up for UDP_PORT 49999 right now, change in the code if neded)

Install the following to run it:

Python 2.7 (2.6 should probably work too)
pyserial
wx
matplotlib
numpy
pylab

To use, simply run:
python plot_port#.py

from the command line.

Note:
Client and Server should have the same UDP_port to be able to communicate each other.
